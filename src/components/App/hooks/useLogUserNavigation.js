import { useEffect } from "react";

const useLogUserNavigation = (pathname) => {
  useEffect(() => {
    if (pathname) {
      //logheaza
    }
  }, [pathname]);
};

export default useLogUserNavigation;
